# IsalaOCR 3.11.6 — reusable panel setup and resize

Panel names are now configured once per project in Step 1. Step 2 only positions those named panels. Snapping to a Paddle region remains a starting point: the selected panel can be resized afterwards with eight edge/corner handles before saving. Clearing panel geometry preserves the configured names.
