# PaddleDetection legacy compatibility

This directory is added to `PYTHONPATH` only for field-localization / PaddleDetection
processes. It intentionally does not affect PaddleOCR recognition training.
